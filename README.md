# secondGenerationSite
Run app.py and use the link from the terminal to open the main page.
Click on the images with molecules to add them to a plot.
Change the data type with the selector above the plot.
